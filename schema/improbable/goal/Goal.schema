package improbable.general;

component GoalParameters {
    // Schema file ID, unique within the project
    id = 1002;

	// Entity's parameters
	float tanksize = 1;
	float tankheight = 2;
	float goalspeed = 3;
	
}