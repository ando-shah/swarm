package improbable.general;

component FishParameters {
	// Schema file ID, unique within the project
    id = 1003;

	// Entity's parameters
	uint32 numfish = 1;
	float initialspeed = 2;
	float tanksize = 3;   //from +tnksize to -tansize in the horizontal plane
	float tankheight = 4; //in the vertical direction, with lower limit implied at 0 for now.
	
}